import React from 'react';
import { Container, Typography } from '@mui/material';

const Attendance = () => {
  return (
    <Container>
      <Typography variant="h4" align="center" gutterBottom>
        Attendance Page
      </Typography>
      {/* UI and logic for managing attendance will go here */}
    </Container>
  );
};

export default Attendance;
